# Caffeine Disable

Disables Caffeine-ng autostart when Waybar's `idle_inhibitor` module is the preferred screen-idle control.

## Apply

```bash
mkdir -p ~/.config/autostart
cp caffeine.desktop ~/.config/autostart/caffeine.desktop
```

Log out and back in, or verify the autostart entry is no longer launching Caffeine-ng.
