# Keep the NVIDIA dGPU available for CUDA and compute workloads on hybrid laptops.
# Dynamic power management lets the dGPU sleep after compute clients exit.
options nvidia NVreg_DynamicPowerManagement=0x02
