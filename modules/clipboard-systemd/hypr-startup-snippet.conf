# Clipboard history is owned by user systemd services:
# ~/.config/systemd/user/cliphist.service
# ~/.config/systemd/user/cliphist-image.service
# Keep these disabled here to avoid duplicate cliphist entries.
# exec-once = wl-paste --type text --watch cliphist store
# exec-once = wl-paste --type image --watch cliphist store
